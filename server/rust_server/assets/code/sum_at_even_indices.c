/*
int sum_at_even_indices(int *array, int size)

Write a function with the prototype above that returns the sum of values at
even indices (0, 2, 4, ...) among the first size elements of array.
If array is NULL or size is lower than or equal to 0, return 0.
Tested sums will fit in an int.
*/
