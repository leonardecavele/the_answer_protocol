/*
int string_compare(char *a, char *b)

Write a function with the prototype above that compares two ASCII strings.
Return -1 if a comes before b, 1 if a comes after b, and 0 if they are equal.
Two NULL pointers are equal. NULL comes before every non-NULL string.
*/
