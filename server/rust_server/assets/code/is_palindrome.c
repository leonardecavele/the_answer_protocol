/*
int is_palindrome(char *str)

Write a function with the prototype above that returns 1 if str reads the
same forward and backward, and 0 otherwise. The comparison is case-sensitive.
An empty string is a palindrome. A NULL pointer is not a palindrome.
*/
