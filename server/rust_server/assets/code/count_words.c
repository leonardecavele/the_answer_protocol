/*
int count_words(char *str)

Write a function with the prototype above that counts the words in str.
A word is a non-empty group of characters separated by one or more ASCII
space characters (' '). If str is NULL or empty, return 0.
*/
