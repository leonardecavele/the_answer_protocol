/*
int array_range(int *array, int size)

Write a function with the prototype above that returns the difference
between the greatest and lowest values in the first size elements.
If array is NULL or size is lower than or equal to 0, return 0.
Tested differences will fit in an int.
*/
