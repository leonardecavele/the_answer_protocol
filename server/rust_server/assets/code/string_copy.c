/*
int string_copy(char *dest, char *src)

Write a function with the prototype above that copies src, including its
terminating null byte, into dest. Return the number of copied characters,
not counting the null byte. If either pointer is NULL, return 0.
You can assume dest is large enough and does not overlap src.
*/
