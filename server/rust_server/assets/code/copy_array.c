/*
int copy_array(int *dest, int *src, int size)

Write a function with the prototype above that copies the first size values
from src to dest and returns the number of copied values.
If either pointer is NULL or size is lower than or equal to 0, return 0.
You can assume the arrays do not overlap and dest is large enough.
*/
