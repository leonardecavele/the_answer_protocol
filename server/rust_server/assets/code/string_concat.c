/*
int string_concat(char *dest, char *src)

Write a function with the prototype above that appends src to dest and
returns the final length of dest. If either pointer is NULL, return 0 and
leave dest unchanged. You can assume dest is large enough and does not
overlap src.
*/
