/*
int string_equals(char *a, char *b)

Write a function with the prototype above
that returns 1 if a and b contain exactly the same characters,
and 0 otherwise.

If either pointer is NULL, return 1 only if both pointers are NULL.
*/
