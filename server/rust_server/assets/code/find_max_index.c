/*
int find_max_index(int *array, int size)

Write a function with the prototype above
that returns the index of the first greatest value
among the first size elements of array.

If array is NULL or size is lower than or equal to 0, return -1.
*/
