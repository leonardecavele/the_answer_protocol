/*
int array_contains(int *array, int size, int value)

Write a function with the prototype above
that returns 1 if value appears in the first size elements of array,
and 0 otherwise.

If array is NULL or size is lower than or equal to 0, return 0.
*/
