/*
int clamp(int value, int min, int max)

Write a function with the prototype above
that returns value limited to the inclusive range [min, max].

If value is lower than min, return min.
If value is greater than max, return max.
Otherwise, return value.

You can assume min is lower than or equal to max.
*/
